# 결함 수정 기록

## 1. 작은 화면에서 내비게이션 소실

### 결함
작은 화면에서 주요 섹션으로 이동하는 내비게이션 전체가 사라졌다.

### 수정 전
850px 이하 미디어 쿼리에서 `nav`를 `display: none`으로 처리했다.

### 수정
화면 폭에 맞춰 간격과 글자 크기를 줄이고, 매우 좁은 화면에서도 핵심 링크 두 개를 유지했다.

### 수정 후
모바일에서도 키보드와 터치로 경험 및 기술 영역에 바로 이동할 수 있다.

## 2. 새 탭 열림 안내 누락

### 결함
GitHub 링크가 새 탭에서 열리지만 화면 읽기 프로그램에는 이 동작이 안내되지 않았다.

### 수정 전
시각적인 화살표만 있었고 화살표는 보조기기에서 숨겨져 있었다.

### 수정
두 외부 링크에 시각적으로 숨긴 `(새 탭)` 문구를 추가했다.

### 수정 후
보이는 디자인을 유지하면서 링크의 동작을 보조기기 사용자도 미리 알 수 있다.

## 3. 동작 줄이기 설정 미반영

### 결함
사용자가 운영체제에서 동작 줄이기를 설정해도 페이지 내 이동에 부드러운 스크롤이 강제됐다.

### 수정 전
모든 환경에서 `scroll-behavior: smooth`가 적용됐다.

### 수정
`prefers-reduced-motion: reduce` 미디어 쿼리에서는 기본 스크롤을 사용하도록 변경했다.

### 수정 후
사용자의 동작 감소 환경설정을 존중한다.
